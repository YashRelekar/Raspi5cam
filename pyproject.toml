[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "raspi5cam"
version = "0.1.0"
description = "Live camera preview for Raspberry Pi"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
